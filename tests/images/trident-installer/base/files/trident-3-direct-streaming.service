[Unit]
Description=Trident Direct Streaming Service
After=network.target network-online.target systemd-udev-settle.service

[Service]
ExecStart=/bin/bash -c "cat /trident_cdrom/direct-streaming.conf | grep HASH | cut -d '=' -f 2 | grep sha384"
Type=oneshot

[Install]
WantedBy=multi-user.target
