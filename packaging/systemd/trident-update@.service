[Unit]
Description=Trident Update (%i)
After=network.target network-online.target tridentd.socket

[Service]
Type=oneshot
EnvironmentFile=/var/lib/trident/update-env
ExecStart=trident update -v trace ${UPDATE_CONFIG} --allowed-operations %i
TimeoutStartSec=600

[Install]
WantedBy=multi-user.target
